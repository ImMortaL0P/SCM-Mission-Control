{
  "name": "scm-mission-control",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "xlsx": "^0.18.5"
  }
}

